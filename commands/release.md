---
description: Open the release PR that carries the base branch into the default branch and closes its issues.
argument-hint: "[--force]"
---

Call the Skill tool with `orchestrator:orch-release` and follow it. The user's
arguments are: `$ARGUMENTS`
